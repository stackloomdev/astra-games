# 设计系统

排版、间距、圆角和层级全部照搬 Airbnb 设计语言；表面反转为深色，品牌色仍然只用 Airbnb 的 Rausch / Luxe / Plus 三支，不引入新色相。令牌都在 [`app/globals.css`](app/globals.css)。

## 字体

Airbnb Cereal VF 是 Dalton Maag 为 Airbnb 定制的专有字体，没有对外授权，无法在本项目使用。西文替换为 **Figtree** —— 同样是几何骨架加圆润字尾，覆盖设计系统依赖的 500–700 字重区间。

Figtree 是全站唯一的网页字体。中日韩、阿拉伯、天城文、西里尔各自走系统字体栈（`--font-script`，按 `:lang()` 切换）。中日韩网页字体是几 MB 的下载，而且为了 unicode-range 分片会在每个页面吐出上千条 `@font-face`；这些脚本在所有目标平台上都已经有像样的系统字体，不值得为此付这个代价。

## 字阶

Airbnb 的层级原样保留，包括字号、字重、行高和字距：

| 角色 | 字号 | 字重 | 行高 | 字距 |
| --- | --- | --- | --- | --- |
| `.t-h1` | 28px | 700 | 1.43 | normal |
| `.t-h2` | 22px | 600 | 1.18 | -0.44px |
| `.t-feature` | 20px | 600 | 1.20 | -0.18px |
| `.t-ui` | 16px | 500 | 1.25 | normal |
| `.t-body` | 14px | 400 | 1.43 | normal |
| `.t-small` | 13px | 400 | 1.23 | normal |
| `.t-badge` | 11px | 600 | 1.18 | `"salt"` |
| `.t-micro` | 11px | 700 | 1.25 | 0.32px, uppercase |

两处偏离，都是为营销页和深色底做的：

- `.t-display` / `.t-section` 用 `clamp()` 沿同一条曲线放大到 Hero 和分区标题的尺度，字距按比例超出 -0.44px。Airbnb 的产品页没有这个尺度的角色。
- `.t-micro` 从 8px 提到 11px。8px 的全大写微型字在深色底上达不到对比度要求；字重、字距和大小写规则保持不变。

原则照旧：标题字重不低于 500，标题带负字距，正文 14px。

## 颜色

品牌色三支不变：Rausch `#ff385c`、Luxe `#460479`、Plus `#92174d`。Luxe 在深色底上提亮为 `#8b5cf6` 才够可读，只用于渐变。

表面按 Airbnb 的"暖，不要纯黑"原则反转：页面底色 `#0d0b0f`（不是 `#000000`），主文字 `#f6f3f0`（对应它的 `#222222` 而不是纯白），次级文字 `#a9a2b4` 对应 `#6a6a6a`。

Rausch 仍然是唯一强调色，只出现在主 CTA、活跃态和品牌标记上，不铺大面积。

## 层级

Airbnb 的三层阴影结构保留 —— 1px 描边环 + 柔和环境阴影 + 主投影 —— 数值按深色底重算：

```css
--shadow-card:
  rgb(255 255 255 / 0.06) 0 0 0 1px,
  rgb(0 0 0 / 0.36) 0 2px 6px,
  rgb(0 0 0 / 0.52) 0 4px 8px;
```

圆角沿用 4 / 8 / 14 / 20 / 32 / 50% 六档，间距基数 8px。

## 动效

Airbnb 本身是克制的、慢节奏的；本站要的是"酷炫"。冲突的解决方式是：**排版和几何严格守 Airbnb，动效自成一层**，不去改字号阴影来"显得动感"。

- Hero：Canvas 2D 轨道粒子场（跟随指针）、极光渐变、逐词入场标题
- 卡片：指针追踪的 3D 倾斜 + 聚光，Airbnb 的整卡点击区不变
- 分区：IntersectionObserver 逐级释放
- 时间轴：GSAP ScrollTrigger 擦除渐变轨道
- 按钮：磁吸位移 + 高光扫过

## 作品卡的两个去向

想直接玩的人和想先看清楚的人是两拨人，卡片同时服务两者：

- **整卡** → 作品详情页（平台、模型参与说明、开发资料）
- **封面上的 Play 按钮** → 直接打开试玩地址，新标签页

实现上不能用「整卡覆盖一个 `<a>`」的写法 —— 那个 `<a>` 会盖住所有控件。改成 stretched-link：标题是真正的链接，用 `::after`（`z-1`）撑满整张卡，Play 按钮 `z-2` 浮在它上面。两者在同一个层叠上下文里，所以 z-index 真的生效。命中已用 `elementFromPoint` 逐卡实测。

没有试玩地址的作品不显示 Play 按钮 —— 按钮的有无本身就是「能不能玩」的信号，所以原来封面右上角那个「可试玩」徽章一并去掉了。

播放三角在 RTL 下**不镜像**：它指的是介质前进方向，不是阅读方向；而「详情 →」的箭头是方向性导航，照常翻转。

## 无障碍与降级

- `prefers-reduced-motion: reduce` 下所有装饰动画停止，没有任何元素停在隐藏态
- 粗指针设备不启用倾斜和磁吸
- 粒子场在离屏或标签页隐藏时暂停，粒子数按视口面积缩放
- `Reveal` 除 IntersectionObserver 外还有 2.5s 兜底计时器；GSAP 全部用 `immediateRender: false`，触发器不响应时元素停在自然状态而不是被压暗
- 中日韩标题按语义短语切分（`.phrase` + `:lang()` 限定 `nowrap`），避免在任意字符处断行；拉丁语系不受影响
- 阿拉伯语整站 RTL：布局用 `start`/`end` 逻辑属性，方向性图标加 `rtl:-scale-x-100`（播放键除外，见上）
- Play 按钮触控区 40px，窄屏 44px
- 以 ` —` 结尾的分句标题用不断行空格粘住破折号，避免窄屏把它挤成独立一行
